using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WeaponManager : MonoBehaviour
{
    private Inventory itemManag;

    // Start is called before the first frame update
    void Start()
    {
        //faire une liste d'effets
        //détailler chaque effet dedans
        //valeur random sur chaque effet
        //valeur random devient celle de l'arme donc donne effet
        //cap de dégâts mis selon l'étage?
        //aaaaaah
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
